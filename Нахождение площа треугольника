class Plos 
	attr_accessor :a, :h
	
		def initialize (a,h)
			@a = a
			@h = h
		end
		def S 
			f = a*h/2	
		end	
end
class Del < Plos
	def initialize (a,h)
			@a = a
			@h = h
		end
end
	puts "Введите а и h" 
	print "a: "
	a = gets.to_i
	print "h: "
	h = gets.to_i
	tr = Del.new(a,h)
	puts tr.S()
